import React, { Component } from 'react'

const Portfolio = () => {
    return (
        <div>
            <h1>Portfolio</h1>
            <button>Portfolio 등록</button>
        </div>
    )
};

export default Portfolio;